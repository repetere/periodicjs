'use strict';
const config = require('./config');
// const controller = require('./controller');
// const db = require('./db');
// const extension = require('./extension');

module.exports = {
  config,
  // controller,
  // db,
  // extension,
};