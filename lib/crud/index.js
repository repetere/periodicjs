'use strict';
const config = require('./config');
// const controller = require('./controller');
// const db = require('./db');
const ext = require('./ext');

module.exports = {
  config,
  // controller,
  // db,
  ext,
};