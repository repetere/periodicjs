language: node_js
node_js:
  - "0.10"

services:
  - mongodb

install: 
  - npm install --prefix ./
  - sleep 10
  - npm run postinstall

branches:
  only:
    - master
    - devel
git:
  submodules: false  
