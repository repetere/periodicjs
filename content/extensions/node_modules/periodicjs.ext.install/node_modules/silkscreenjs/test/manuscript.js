/*
 * manuscript
 * http://github.com/typesettin/manuscript
 *
 * Copyright (c) 2014 Yaw Joseph Etse. All rights reserved.
 */

'use strict';

var should = require('chai').should(),
	expect = require('chai').expect,
	// Manuscript = require('../lib/manuscript'),
	path = require("path");

// var manuscriptFactory = {
// 		create: function(overwrites){
// 			return Object.create(new Manuscript(overwrites));
// 		}
// 	};

describe('DOM Manipulation', function () {

	describe('Initializing Page Settings', function () {
		// var manuscript;

		// it('should return configuration object', function () {
		// 	manuscript = manuscriptFactory.create();
		// 	manuscript.config().should.be.a("object");
		// });

		// it('should allow you to overwrite default options',function(){
		// 	manuscript = manuscriptFactory.create({resize:false});
		// 	should.equal(manuscript.config().resize,false);
		// });

		it('should wrap each section with a table container if vertically centered',function(){});

		it('should wrap each slide with a table container if vertically centered',function(){});

		it('should activate anchor link of active slide',function(){});

	});

});
