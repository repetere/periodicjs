<input type="hidden" name="_csrf" value=<%= locals.token %> >
<div class="_pea-container">
	<div class="_pea-row">
		<div class="_pea-col-span6">
			<% 
				var genericdoc = userrole; 
				var genericdocTitle = "User Role"; 
			%>
			
			<h2><% if(genericdoc){%>Edit<%}else{%>New<%} %> <%= genericdocTitle %></h2>
			<%if(genericdoc){%>
				<input type="hidden" name="docid" value="<%= genericdoc._id %>">
			<%}%>
			<div class="_pea-document">
				<div class="_pea-row">
					<div class="_pea-row _pea-container-forminput">
						<input class="_pea-col-span12 noFormSubmit" type="text" placeholder="Title" value="<% if(genericdoc && typeof genericdoc.title==='string'){%><%= genericdoc.title %><%}%>" name="title"/>
					</div>
					<div class="_pea-row _pea-container-forminput no-margin">
						<textarea id="wysihtml5-textarea" class="textarea-tall _pea-col-span12" name="content" placeholder="New <%= genericdocTitle %>"><% if(genericdoc && typeof genericdoc.content==='string'){%><%= genericdoc.content %><%}%></textarea>
					</div>
				</div>
			</div>
		</div>
		<div class="_pea-col-span6 _pea-pull-right">
			<h2>Privileges</h2>
			<div class="_pea-document">
				<div class="_pea-row _pea-container-forminput">
					<input type="text" id="padmin-privileges" class="_pea-col-span12" placeholder="Type" name="privileges"/>
				</div>
			</div>
		</div>
	</div>
</div>
